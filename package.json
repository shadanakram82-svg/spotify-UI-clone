{
  "name": "spotify-clone",
  "version": "1.0.0",
  "description": "Spotify UI Clone",
  "scripts": {
    "dev": "browser-sync start --server --files \"**/*.html, **/*.css, **/*.js\" --no-notify"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3"
  }
}
